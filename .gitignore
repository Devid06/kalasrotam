node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# The original WhatsApp brief. Private correspondence — keep it out of the repo,
# especially if the repo is public.
read.md

# Full-resolution source art. design/logo-full.png is the 2.3MB original the
# 120KB public/images/logo.png was made from — kept on disk so it is never lost,
# kept out of git so the repo does not carry it forever.
design/

# Editors
.vscode/*
!.vscode/extensions.json
.idea

# Logs
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local secrets
.env
.env.local
.env.*.local
